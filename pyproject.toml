[project]
name = "txt2svg"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fal-client",
    "requests",
    "scikit-image",
    "opencv-python",
    "numpy",
    "Pillow",
    "networkx",
    "scipy",
    "vpype",
    "openai",
    "python-dotenv",
]

[project.optional-dependencies]
dev = [
    "pytest",
]
local-gpu = [
    "torch",
    "diffusers",
    "transformers",
    "accelerate",
    "sentencepiece",
    "protobuf",
    "bitsandbytes",
]

[tool.setuptools.packages.find]
include = ["modules*"]
